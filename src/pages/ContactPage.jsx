import React from 'react';
// We are simply importing the Contact component you already made
import Contact from '../components/Contact';

function ContactPage() {
  // The Contact component already has its own padding and structure.
  return <Contact />;
}

export default ContactPage;